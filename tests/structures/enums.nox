// Assert that basic Enum functionality works

enum Colors {
  Red,
  Green = 3,
  Blue  = 5,
}

func int main() {
    color_red = Colors.Red
    color_green = Colors.Green
    color_blue = Colors.Blue

    // 1, 3, 5
    print("%d, %d, %d\n", color_red, color_green, color_blue)

    return 0
}